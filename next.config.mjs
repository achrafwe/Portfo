/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:"/Portfo",
    output: 'export', 
     reactStrictMode:false,
     images: {
      unoptimized: true,
  },

};
  
  export default nextConfig;
  
